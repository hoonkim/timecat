[package]
name = "timecat"
version = "0.1.2"
edition = "2024"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
crossterm = "0.29"
serde_json = "1"
unicode-width = "0.2"
